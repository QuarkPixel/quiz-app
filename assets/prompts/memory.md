请你按照以下格式帮我整理记忆卡片，用 `json` 代码块输出：

```json
{
  "mode": "memory",
  "title": "英语短语",
  "questions": [
    {
      "id": "memory_1",
      "question": "取得进步",
      "answer": "make progress"
    },
    {
      "id": "memory_2",
      "question": "地球是太阳系中最大的行星。",
      "answer": "错。木星才是太阳系中最大的行星。"
    },
    {
      "id": "memory_3",
      "question": "《静夜思》的作者是谁？",
      "answer": "李白"
    },
    {
      "id": "memory_4",
      "question": "简述牛顿第一定律的内容。",
      "answer": "一切物体在没有受到外力作用时，总保持静止状态或匀速直线运动状态，直到有外力迫使它改变这种状态为止。"
    },
    {
      "id": "memory_5",
      "question": "TCP 三次握手分别做了什么？",
      "answer": "第一次握手：客户端发送 SYN，带上自己的初始序号 seq=x，进入 SYN_SENT。\n第二次握手：服务端回复 SYN + ACK，带上自己的 seq=y 与 ack=x+1，进入 SYN_RCVD。\n第三次握手：客户端回复 ACK，ack=y+1，双方进入 ESTABLISHED。\n为什么不是两次：若只有两次，服务端无法确认客户端的接收能力，历史上延迟到达的 SYN 还会让服务端建立无效连接。"
    }
  ]
}
```

注意：

- `"mode": "memory"` 指明当前为记忆模式，请不要省略。
- `"title"` 是可选字段：你可以顺手给这份题库起一个简短的标题（导入后会作为题库名称显示）；不想起也可以省略，省略时用文件名作为名称。
- `questions` 字段内为实际题目，在里面生成对应内容即可。
- 内部的 `id` 字段不一定要按照给定的格式来，只要保证唯一即可。
- `answer` 用 `\n` 分段，其余内容均已纯文本的形式展现。

以下是内容：

---
